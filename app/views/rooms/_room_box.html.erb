<div class="item" id="<%= @id %>">
          <h4><%= @name %></h4>
          <br>
          <h5><%= @description %></h5>
          <%= form_tag("/users/#{room.user_id}/rooms/#{room.id}", method: :delete, remote: true) do %>
            <div class="delete"><%= submit_tag 'Delete' %>
            </div>
          <% end %>
</div>
