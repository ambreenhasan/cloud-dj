$(document).ready(function() {

  $(".item").on("click", function() {
    var room_id = this.id
    // alert(room_id)

    $.ajax ({
      url: "/users",
      type: "GET",
      data: {room_id: room_id}
    }).done(renderRoom).fail(function(){alert("fail")})
  })


});

  function renderRoom() {
    alert("hohohho");
    $("#ajax_home_thing").remove();
    // $("#room").slideDown(1000);
    $("#home_body").append(""<%= j (render 'rooms/room') %>"");
  }
